<img src="" alt="加载失败提示信息"/>
<s>strike line<s> 删除线标签

text-decoration:none 无装饰，通常对html下划线标签去掉下划线样式
text-decoration:underline 下划线样式
text-decoration:line-through 删除线样式-贯穿线样式
text-decoration:overline 上划线样式

border-radius: 5px;
border-top-left-radius:5em;
border-top-right-radius:5em;
border-bottom-right-radius:5em;
border-bottom-left-radius:5em;

overflow: 当内容溢出元素框时的行为
    hidden
    visible
    scroll
    auto
    inherit
display: 规定元素应该生成的框的类型
    block
    inline
    inline-block
    none
    list-item
    run-in
    compact
    marker
    table
    inherite-table
    table-row-group
    table-header-group
    table-footer-group
    table-row
    table-column
    table-column-group
    table-cell
    table-caption
    inherit



visibility: 规定元素是否可见, 即使不可见的元素依然
            会占用页面空间
    visible
    hidden
    collapse
    inherite: 从父元素继承visibility的值

position
    static
    fixed
    absolute
    relative

html5
    audio
        src
        autoplay
        controls

    video    
        src
        controls


<marquee>滚动标签

table @align @cellspacing @border
    caption: 标题
    thead: 表头
    tbody
        tr @align @valign
            td @colspan @rowspan @bgcolor @background image        

browser object model(BOM)
    window
        document(DOM)
        history
        location
        frames
        navigator
        screen

input type
    button
    email
    url
    color
    checkbox 复选框
    datetime-local
    number
        @max @min //有submit提交按钮可自动检查 
    password 
    range
    text
    radio 单选框
    submit

input是行内元素

float 脱离文本流， 使块元素成为行内元素
float元素后面的， 必须要clear both


<body onload="initialize()"> 页面加载完毕后触发

span width
	display: inline-block
    inline 不能设置宽度， block自带换行
    inline-block可兼容两种情况

input range onchange onmousemove

如果某些元素没有设置样式， 则自动从父元素继承
样式的定义有严格的先后顺序, 当发生重复时， 后定义的样式会覆盖前面的样式
